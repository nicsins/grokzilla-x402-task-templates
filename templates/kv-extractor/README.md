# x402 Key-Value Field Extractor

$0.016 USDC per extraction via x402. Pulls clean key-value pairs from free-form text using robust patterns.

Stateless, agent-native, zero human in the loop. Deploy: set PAY_TO_WALLET → npm start or Vercel.

POST /extract
{ "text": "..." }
