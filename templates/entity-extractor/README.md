# x402 Entity & Relation Extractor

$0.025 USDC per call via x402. Returns clean JSON entities + relations.

Deploy: set PAY_TO_WALLET → npm start or Vercel.

Endpoint: POST /extract  { "text": "..." }
