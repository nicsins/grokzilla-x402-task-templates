# x402 Temporal Event Timeline Extractor

$0.018 USDC per extraction via x402. Pulls ordered events with relative/absolute time cues into a clean timeline JSON.

Stateless, agent-native, zero human in the loop. Deploy: set PAY_TO_WALLET → npm start or Vercel.

POST /timeline
{ "text": "..." }
